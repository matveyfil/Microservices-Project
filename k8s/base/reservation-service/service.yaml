apiVersion: v1
kind: Service
metadata:
  name: reservation-service
  namespace: restaurant
spec:
  selector:
    app: reservation-service
  ports:
    - name: http
      port: 8004
      targetPort: 8004
